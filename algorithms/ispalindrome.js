function isPalindrome(str1,str2){
    if(str1.length < 1 || str2.length < 1) return 'undefined';

    const trimmedStr1 = str1.trim();
    const trimmedStr2 = str2.trim();
    const reversedStr1 = trimmedStr1.split("").reverse().join("")
    //if()
}